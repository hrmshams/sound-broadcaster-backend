{
  "name": "sound-broadcaster-server",
  "version": "1.0.0",
  "description": "an api for broadcasting the sound and or text messages for using in chat ",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "hamid shams",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.16.4",
    "socket.io": "^2.2.0"
  }
}
